# Meeting Notes April 23, 2026

## Starting the project/problem

### How to get the $P$ matrices

- If doing finite elements, they are just there. *They are generated by `.refine` and are based on the geometry, we need to figure out how to capture them.* 
    + You have a coarse mesh,
    + Call a refinement,
    + The refinement is the $P$ matrix.
- Vertices of one are a subset of another. $P$ will map from the coarse to finer meshes.
- How to get $P$ matrix from NGSolve after doing refinement
- Check that $P^T A P$ gives you coarse matrix from fine
- Use geometric $P$ from NGSolve *dig into the backend to understand the implementation of `.refine`*

> Smoothing?  
    Coarse grid correction

Extra oscillations that need to be smoothed out.  

Run geometric two based Monte Carlo method. *Model the example from his book, but the $x$ should have two parts, one part smoother frequency and the other a highly oscillatory one.*  

Compute diff from coarse to fine  

*Monte Carlo sampling space based, piecewise constant coefficients*  

*Subsurface flow in porous media*  

Two-level Monte Carlo convergence depends on *variance*  

### PDEs to solve

- Laplace equation with coefficients (Poisson equation) 
    + One boundary set at 0, one with flux b.c.
- Coefficients are $\log$ normal distribution

**Put a title on our project proposal**

We will be doing two-level Monte Carlo method for subsurface flow that is governed by Poisson equation with random coefficients. Generating discretization over a sequence of meshes, generating the matrices and $P$ matrices. Working in $H^1$ piecewise linear.  

*Stay out of the other finite element spaces for now, maybe in future research*  

Procedure to solve the two-level.  

$x$ has a smooth component and an oscillatory component.  

### Gauss-Seidel method
