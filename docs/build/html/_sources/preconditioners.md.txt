# Preconditioners

The Python wrapper calls the preconditioners from the C++ code, so the documentation will come when we include that part of it.

<!-- Using the autodoc tool -->

```{eval-rst}
.. automodule:: ngsolve.preconditioners
  :members:
  :undoc-members:
```
